{
  "name": "workers",
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "import-scryfall": "tsx run.ts",
    "tsc:check": "tsc --noEmit",
    "format:fix": "prettier --write . --ignore-path ../.prettierignore",
    "format:check": "prettier --check . --ignore-path ../.prettierignore",
    "lint:fix": "eslint . --fix",
    "lint:check": "eslint ."
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.94.1",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "vitest": "^4.1.1"
  }
}
